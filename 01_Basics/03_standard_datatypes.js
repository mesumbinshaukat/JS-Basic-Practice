"use strict"; //It strictly follows new code practices in JS according to the latest version of JS.

let userName = "Mesum" // Datatype = String
let userPassword = 778866 // Datatype = Number
let userAge = 18.7 // Datatype = Number
let isUserActive = true // Datatype = boolean
let userCountry // Datatype = undefined
let userWeight = null // Datatype = Object, null is also known as standalone value

console.table([typeof userName, typeof userPassword, typeof userAge, typeof isUserActive, typeof userCountry, typeof userWeight])